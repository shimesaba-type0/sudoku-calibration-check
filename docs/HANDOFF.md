# 引き継ぎ手順 — 数独キャリブレーションチェック

最終更新: 2026-09-21

このファイルは「人が手を離しても、Claude Code が Issue → 実装 → テスト → レビュー → マージ のループを回し続けられる」ための手順書。セッションを引き継ぐときは、`CLAUDE.md` → `docs/SPEC.md` → `docs/DESIGN.md` → このファイルの順に読む。

## 1. 人間にしかできない作業(未完了なら最初に依頼する)

Worker アプリ自体は `wrangler deploy` が自動で作るので、Cloudflare のダッシュボードで「アプリを作る」操作は不要。GUI が必要なのは 1.1〜1.3(Cloudflare ダッシュボードと Claude Code の環境設定)と 1.5(GitHub の設定)。レート制限のカウンタ(1.4)は Durable Object になったので手作業は不要。

| 作業 | 状態 |
|---|---|
| 1.1 API トークンと Account ID を取得 | 済(2026-09-21) |
| 1.2 workers.dev サブドメインを登録 | 済(2026-09-21) |
| 1.3 Claude Code のクラウド環境にトークンと環境変数を設定 | 済(2026-09-21) |
| 1.4 レート制限のカウンタ(Durable Object)| 不要(`wrangler deploy` が作る) |
| 1.5 デフォルトブランチを `main` にする | 済(2026-09-22。マージ時のブランチ自動削除もオン) |

### 1.1 API トークンと Account ID(Cloudflare ダッシュボード)

1. https://dash.cloudflare.com/profile/api-tokens → 「Create Token」→ 一番下の「Create Custom Token」→「Get started」
2. Token name: `sudoku-calibration-check-deploy`(任意)
3. Permissions に4行追加: `Account / Workers Scripts / Edit`、`Account / Workers KV Storage / Edit`、`Account / Workers AI / Read`、`Account / Account Settings / Read`(`Workers Scripts / Edit` に Durable Objects の作成も含まれる。`Workers KV Storage / Edit` は現在は使っていないが、付けたままで害は無い)
4. Account Resources: Include → 自分のアカウント
5. 「Continue to summary」→「Create Token」。トークンは一度しか表示されないので控える。**チャットやリポジトリには貼らない**
6. Account ID: https://dash.cloudflare.com → Workers & Pages → 右側「Account details」

### 1.2 workers.dev サブドメイン(初回のみ)

Workers & Pages → Overview の右側に「Your subdomain」が無ければ「Set up a subdomain」で登録する。非対話の `wrangler deploy` はこれが無いと失敗することがある。

### 1.3 Claude Code のクラウド環境(claude.ai/code → Settings → Environments)

2026-09-21 に次の形で設定し、これで通ることを実環境で確認した。**環境変数にトークンの実物を入れない**のがポイント。

1. **トークンは「API 認証情報」として登録する**。環境設定の API 認証情報(API credentials)に、ホスト `*.cloudflare.com` 宛ての Bearer トークンとして 1.1 のトークンを登録する。こうするとセッションのプロキシがリクエストに注入してくれるので、セッション自身(= Claude Code)はトークンの値を一度も見ない
2. **環境変数** は次の3つだけ:

````text
CLOUDFLARE_API_TOKEN=placeholder
CLOUDFLARE_ACCOUNT_ID=<1.1 の Account ID>
WRANGLER_SEND_METRICS=false
````

`CLOUDFLARE_API_TOKEN` はダミーの文字列でよい。wrangler は「トークンが設定されていない」と非対話実行を拒否するので、その判定を通すためだけに置く。実際の認証は 1 のプロキシ注入で行われる。

3. **ネットワーク(egress)の許可リスト** に次を入れる。
   - `api.cloudflare.com` / `*.cloudflare.com` — 無いと `wrangler deploy` / `wrangler kv` が動かない
   - `*.workers.dev` — デプロイした Worker を `curl` で動作確認するのに要る(`CLAUDE.md` 作業ルール5)

4. **AI Gateway の課金を有効にする**。`typesafe/jev` は Workers AI のニューロンではなく **AI Gateway のクレジット** で課金される(`docs/DESIGN.md` 3.4)。ダッシュボード → AI → AI Gateway で課金を有効にしていないと、デプロイは通るのに `/api/judge` だけが失敗する。

### 1.4 レート制限のカウンタ(Durable Object)

**手作業は不要**。レート制限のカウンタは Durable Object(クラス `RateLimitCounter`、バインディング `RATE_LIMITER`)で、`wrangler.toml` の `[[migrations]]` を見て `wrangler deploy` が作る。ダッシュボードでの事前作成も `id` の貼り付けも要らない。

以前は Workers KV(`RATE_LIMIT_KV`)を使っており、ネームスペースを1回作って `id` を `wrangler.toml` に貼る手作業があった。KV は結果整合で全体上限が分散アクセスに効かなかったため、Issue #10 で Durable Object に移行した(`docs/DESIGN.md` 3.2)。**古い KV ネームスペースはそのまま残っている**ので、不要なら Workers & Pages → KV から削除してよい(消さなくても害は無い)。

注意: SQLite バックエンドの Durable Objects は Workers **Free** プランでも使える(`new_sqlite_classes` で登録しているのはそのため)。ただしプランごとの上限は変わることがあるので、デプロイがプラン関連のエラーになったらダッシュボードで確認する。1 判定あたりのカウンタ操作は最大3回(全体 peek / IP +1 / 全体 +1)。以前の KV(書き込み 1,000 回/日で 1 日約 500 判定が上限)のような日次の書き込み制限に当たる心配は無くなった。

### 1.5 デフォルトブランチ

GitHub → Settings → General → Default branch → 鉛筆アイコン → `main` を選んで Update → 確認。その後 Branches ページで不要な旧ブランチを削除。

トークンが無いセッションでは deploy をせず、`npm test` と `npm run check` までで止めて報告する(`CLAUDE.md` 作業ルール5)。

## 2. 自律ループ

1. **Issue を選ぶ**: `status:ready` ラベルの付いた open Issue のうち番号が小さいものから。無ければ `docs/SPEC.md` 7章の「今後の拡張」から次を Issue に起こす(受け入れ基準を必ず書く)
2. **ブランチを切る**: `main` から `claude/issue-<番号>-<短い英語名>`。Issue 番号を必ず含める
3. **実装する**: 担当モデルは 3 章の表に従う。実装者には Issue 本文・関連ドキュメントの節・触ってよいファイルを明示して渡す
4. **検証する**: `npm test` → `npm run check`。落ちたら直す。テストを消したり skip したりして通すのは禁止
5. **PR を作る**: base は `main`。本文に「Closes #<番号>」、変更点、テスト結果(コマンドの出力)、確認できなかったことを書く。Draft で作り、レビュー後に Ready にする
6. **レビューする**: 実装者とは別のエージェントに、PR の diff と関連ドキュメントを渡してレビューさせる。レビュー観点は 4 章。指摘は実装者(または同じ担当帯のエージェント)が直し、再レビュー。指摘ゼロになるまで繰り返す
7. **マージする**: CI(GitHub Actions)が緑で、レビュー指摘がゼロなら squash merge。Issue は「Closes」で自動クローズ
8. **状態を更新する**: `CLAUDE.md` の「現在の状態」と、このファイルの 6 章「現在地」を直す。設計が変わったなら `docs/DESIGN.md` も同じ PR で直す(`CLAUDE.md` 作業ルール3)
9. 1 に戻る

ループを止める条件: 人間にしかできない作業(1 章)でブロックされた / 仕様の曖昧さで判断が割れる(`CLAUDE.md` 作業ルール8に従い質問として報告する) / レート制限を緩める変更が必要になった(作業ルール4)。

**進行役は 1 セッションだけ。** このループ(Issue 起票・ブランチ作成・PR 作成・マージ・ドキュメントの現在地更新)を回すのは親セッション 1 つに限る。親セッションが起動した子セッション(デプロイ確認など)や実装・レビューのサブエージェントは、渡された手順だけを実行して報告し、Issue や PR を自分で作らない・マージしない。同じ Issue を 2 つのセッションが並行して実装すると、重複 PR と競合が生じ、コストも倍になる(2026-09-21 に実際に起きた)。

**複数セッションが同時に走る場合の調停ルール**(進行役が複数存在しうる状況、または進行役と独立に動くセッションが並行する状況向け):

- 着手前に対象 Issue へ「着手中(セッション ID)」とコメントする。コメントしてから作業を始める
- 同じ Issue に 2 つの PR を出さない。先にコメントした方が担当。後から気づいたセッションは着手を控え、必要ならコメントで調整する
- `src/index.js` の `PAGE_HTML`(フロントエンド部分)を触る変更は直列にする(同時に別セッションが編集すると衝突しやすいため)。Worker 側コード・`docs/` 配下・新規ファイルの追加は、対象ファイルが重ならない限り並列でよい
- 別セッションが出した PR を閉じる場合は、閉じる前に理由をその PR にコメントする

## 3. モデルの使い分け

方針(オーナー指示 2026-09-21): 基本は Sonnet。判断が重い箇所と**全 PR のレビューは Opus**。**Fable は本当に重要なところだけ**に絞る(Fable の利用枠が限られるため)。

| 作業 | モデル | 理由 |
|---|---|---|
| フロントエンド(`PAGE_HTML`)、README、ドキュメント整備、数独ジェネレーター/ソルバーなどの生成器、テスト追加、小さな修正 | Sonnet | 通常の実装作業であり、判断の重さも低いため |
| `handleJudge`・`checkRateLimit`・入力検証など、コストと実験の正しさに直結する Worker 側コードの実装 | Opus | 崩れると課金やレート制限の前提が壊れる、判断の重い実装のため |
| PR レビュー(全 PR。実装者が Sonnet でも Opus でも) | Opus | レビューは実装者と同格以上のモデルで行う方針のため |
| 認証・秘密情報・ネットワーク境界に関わる変更 | Fable | 事故ると秘密情報漏洩やアクセス制御の穴に直結する、特に重い判断のため |
| レート制限の上限変更(`RATE_LIMIT_PER_IP_MAX` / `RATE_LIMIT_GLOBAL_MAX` を緩める、`RATE_LIMITER` バインディングを外すなど)とそのレビュー | Fable | コストの青天井を防ぐ唯一の砦(`CLAUDE.md` 作業ルール4)であり、緩める判断は特に重いため |
| 課金に関わる変更(AI 呼び出し経路・レート制限カウンタの仕組みの変更)とそのレビュー | Fable | Jev の呼び出しはコスト・実験の正しさに直結し、仕組みを変えるミスの影響が大きいため |
| 複雑な設計判断(アーキテクチャの変更、不変条件に関わる判断など) | Fable | 一度の判断ミスの手戻りコストが大きいため |
| Jev のレスポンス形式が公式と違った場合の `handleJudge` と `DESIGN.md` 3.4 の同時修正 | Opus | 実験の正しさに直結する Worker 側コードの修正のため |
| ループ全体の進行管理、Issue 起票、マージ判断 | セッション本体(Opus 以上) | 複数 Issue・PR を横断する意思決定のため |

## 4. レビュー観点

- `docs/DESIGN.md` 9章の不変条件をすべて満たしているか(特に 1・7: `SOLUTION` が Worker の判定コードや Jev の `state` に混ざっていないか)
- 入力検証が `DESIGN.md` 3.3 の項目を網羅しているか
- レート制限の順序(全体 peek → IP +1 → 全体 +1)、拒否したときに加算しない条件、`[vars]` からの読み取り、フェイルオープン/フェイルクローズが設計どおりか
- エラー時に必ず `running=false` になるか(フロント)
- テストが「通るように書かれた」ものでなく、失敗すべき入力で失敗しているか
- `wrangler.toml` に `account_id` や秘密情報が入っていないか
- ドキュメントと実装が食い違っていないか(食い違うなら同じ PR で直す)

## 5. ブランチ・PR・コミットの規約

- ブランチ: `claude/issue-<番号>-<短い英語名>`
- コミットメッセージ: 日本語可。1行目は `<種別>: <要約>`(種別は `feat` / `fix` / `docs` / `test` / `chore` / `ci`)
- PR タイトル: コミットの1行目と同じ形式。本文に `Closes #<番号>`
- マージ方法: squash
- Issue のラベル: `status:ready`(着手可)/ `status:blocked`(人間待ち。理由を本文に書く)/ `type:feature` / `type:verify` / `type:docs`

## 6. 現在地

- 2026-09-21: ドキュメント(SPEC / DESIGN / CLAUDE / HANDOFF)を整備。v0.1 の実装を Issue に分割して着手
- 2026-09-21: 人間作業 1.1〜1.4 が完了。KV ネームスペースを作成して `wrangler.toml` に反映し、初回デプロイを実施(https://sudoku-calibration-check.takashi-kono-rb.workers.dev)。`GET /` はフロントエンドの Issue が入るまで「実装中」の仮ページ
- 2026-09-21: **Jev を実環境で初めて検証**。レスポンスが AI Gateway のラッパー(`{ state, result, gatewayMetadata }`)で返ってくることが判明し、`handleJudge` と `docs/DESIGN.md` 3.4 / `docs/SPEC.md` 4・5章を実測に合わせて修正。併せて分かったこと: `confidence` は `probabilities[choice]` と一致しない / 1 判定あたり約 670 入力トークン / Worker 経由のレイテンシは 1.0〜1.7 秒 / 同じ入力でも `choice` が揺れる(決定的でない)
- 2026-09-21: フロントエンド(Issue #3、PR #13)をマージ。グリッド・判定パネル・周回ロジック・速度モード・実行世代トークン(リセット直後の再実行で判定が二重化しない)を実装。モック API + Playwright で周回・15 周打ち切り・エラー停止を確認。v0.1 の機能はこれで揃った
- 2026-09-21: 検証用の子セッションが範囲を超えて独自に PR #11(マージ済み、内容は妥当)と PR #12(重複のため閉鎖)を作った。再発防止として 2 章に「進行役は 1 セッションだけ」を追記
- 2026-09-21: レート制限のカウンタを Workers KV から Durable Object(`RateLimitCounter` / `RATE_LIMITER`)に置き換え(Issue #10)。KV の結果整合による lost update で全体上限が分散アクセスに効いていなかった問題を解消。上限値(30 / 500 / 3600)は据え置きで、**緩める変更ではない**(`docs/DESIGN.md` 3.2)。初回デプロイ時に `[[migrations]]` で DO クラスが作られる
- 2026-09-21: PR #14(レート制限を Durable Object に置き換え)をマージ・デプロイ(Version 7f6d90b7)。合わせて PR #7(Worker 本体)・#9(Jev 実レスポンス形式 + KV id)・#11(#7/#9 のレビュー指摘)もマージ済み。本番 URL でフロントエンド + DO レート制限が稼働中
- 2026-09-21〜22(子セッション、オーナー指示で自律進行): Issue を起票しつつ、衝突しない単位(docs / 新規ファイル / Worker 側 / `PAGE_HTML`)で並列実装。`PAGE_HTML` を触る変更は直列にした。マージ順: #22(モデル方針 docs)→ #23(`GET /api/status`)→ #15(ジェネレーター/ソルバー。Opus レビューの must-fix「実行中にリセットが押せない」を修正してから)→ #24(Playwright E2E スモーク)→ #25(集計ビュー。Opus レビューの should-fix 3 件を修正: 読み取り失敗時の全消し防止・キャッシュ化・テスト高速化)→ #26(README)→ #27(AbortController + 振る舞いテスト S1〜S4)→ #28(難易度トグル)。各マージ後にデプロイし、`npm run e2e -- --url <URL>` の本番モードで確認。最終 Version は `2b8eb12c`
- 2026-09-22: **v0.1 と SPEC 7 章の拡張 1・2・5(最小版)まで完了**。開いている Issue はオーナー判断待ちの #20 のみ
- 2026-09-22: 人間作業 1.5 完了。デフォルトブランチが `main` になり、マージ時のブランチ自動削除もオン。残っている人間作業は無し(過去のマージ済みブランチの削除はセッションの git プロキシが ref 削除を許可しないため、オーナーが手元で `git push origin --delete ...` を実行する)
- 2026-09-22: オーナー判断(Issue #20)で IP 単位のレート制限を 30 → 120 回/時に緩和(`wrangler.toml` の `[vars]` のみ変更、全体 500 は据え置き)
- 2026-09-22: オーナー要望で「停止/再開」(Issue #32、PR #33)をマージ・デプロイ(Version `f34501a4`)。本番で Playwright により停止→再開を確認(停止後は `/api/judge` が飛ばない、パネルは「停止中(残り N マス)」)
- 2026-09-22: オーナー要望で「Jev に送ったプロンプト」の表示枠(Issue #34、PR #35)をマージ・デプロイ。Worker が `env.AI.run` に渡した `payload` を `request` として返し、フロントはそれを表示するだけ(二重管理しない)。Opus レビューの should-fix 3 件(README の API 表・502 の `request` をパネルに出す・E2E モックの契約追随)を反映。実装のサブエージェントが PR 前に本番へデプロイしてしまったが、内容はマージしたものと同一
- 2026-09-22: オーナー要望 3 件。(1) 盤面の表現(文字列 vs 配列/ハッシュ/集合)は Issue #36 に検討事項として記録(着手は別途判断)。(2) Claude(Anthropic API)対応は Issue #37 → PR(BYOK・ブラウザ直呼び・structured outputs で自己申告確率。記録に `m`、較正図にモデルフィルタ)。実キーでの疎通はオーナーのブラウザで確認する。(3) 確信度順モードは Issue #38(1 マス 2 回の呼び出しになりレート制限との兼ね合いあり)
- 2026-09-22: 3 件を順にマージ・デプロイ。#39(Claude API、BYOK。Opus レビューの must-fix「adaptive thinking の max_tokens」を反映)→ Version `9feebd7b`。#40(`/api/judge` の `ask:"cell"`。Worker 側)→ Version `540034b3`。本物の Jev で 51 択(ふつう = 30 ヒントの空マス数)のマス選びが 200 / 約 2 秒 / 確率合計 1.0 で返ることを確認(分布はほぼ平坦、最大 0.07)。#41(確信度順モードのフロント。Opus レビューの must-fix「E2E [7] の総数一致が mixed モードで不安定」を反映)→ Version `51abedb7`。実ブラウザ(Playwright + モック)で 2 段ループ・ヒートマップ・停止/再開を確認
- 2026-09-22: 運用メモ。セッションのプロキシ経由だと Playwright(Chromium)の本番モード(`npm run e2e -- --url`)が `ERR_TOO_MANY_RETRIES` で落ちることがある(1 リクエスト 5〜8 秒かかり、2 件目以降で失敗)。Worker 側は 200 を返しており(curl は 1〜2 秒)、コードの問題ではない。再実行で通ることもある
  - 確実に見たいときの回避: GET / と POST /api/judge を `curl` で本番に転送する `http.createServer` をローカルに立て、そこに `page.goto` して Playwright を走らせる(`scripts/e2e/out/` は gitignore なので手順のみ)
- 2026-09-22: 実装済みなのに開いたままだった Issue(#5 #6 #10 #16 #17 #18 #19 #21。スカッシュマージで `Closes` が効かなかったもの)を閉じた。**PR をマージしたら Issue が閉じたかを確認する**
- 2026-09-22: オーナー提案の「数字ごと」(#45)の Worker 側 `ask:"where"`(PR #47)をマージ・デプロイ(Version `6642e162`)。Jev は 1 回の呼び出しに空マスぶんの `noul` 質問(51 個)を入れられることを実機で確認。本番で `digit:"4"` を 1 回叩き 200 / 約 1.4 秒 / 51 件。ただし分布は平坦(正解マス 0.22 vs それ以外 0.22)。フロントは #48「一括」との兼ね合いでオーナー判断待ち
- 2026-09-22: 比較モード(#46、PR #49)をマージ。同じページを iframe 2 枚(Jev / Claude)で並べ、`/compare` の上部バーから postMessage で同時操作。CSP は `frame-ancestors 'self'`。Opus レビューの must-fix「2 枚が記録(`scc.records.v1`)を互いに上書きする」を `appendRecord()` の毎回読み直しで修正
- 2026-09-22: オーナー判断 #43 = 「一時的な失敗は停止扱い」で確定(未実装。#49 の後に着手)。「一括」モード(#48)を起票(Jev は 51 個の choice 質問を 1 回で返せることを実機で確認: 1.1 秒、入力 9,483 / 出力 4,083 トークン)
- 2026-09-22: #43 実装(PR #51)をマージ・デプロイ(Version `0057680a`)。Opus レビューの should-fix 3 件(`Retry-After` 欠落時の「0秒後」表示・Y1 の空振り・`stop()` と `pauseForTransientError()` の重複 → `haltRun()`)と nit(Y6〜Y8 追加)を反映してからマージ。本番は curl 中継の Playwright で 2 マス判定 → 停止まで確認(停止扱いそのものは本番で強制できないのでテスト/E2E `--mode ratelimit` で担保)。Jev 経路(Worker)の 429/503、Claude 経路の 429/529/5xx・ネットワーク失敗を `pauseForTransientError()` で「停止」(#32 と同じ後始末)扱いにし、`errorMessage` を立てず `state.pauseReason` に理由を持たせた。それ以外(400/401/415/502/形式不正など)は従来どおりエラー停止。振る舞いテスト Y1〜Y8、E2E モック `--mode ratelimit` の [4] を更新
- 2026-09-22: #48(一括モード)の Worker 側 `ask:"all"`(PR #52、Opus 実装)をマージ・デプロイ(Version `29b36d48`)。Opus レビューの should-fix 2 件(文言の注入口・SPEC の質問数上限)と nit を反映してからマージ。本番で固定問題(空マス 51)を 1 回 curl: 200 / 1.6 秒 / 23 KB、`cells` 51 件、正解 12/51、確率の最大 0.39、confidence 0.05〜0.32(分布はほぼ平坦。探り用 Worker の実測 11/51 と同程度)。フロント(モードのトグル・表示・記録の `m` の分け方)は別 PR。オーナー判断待ち(A/B と `m` の別名方式)
- 2026-09-22: オーナー判断で一括モード(#48)のフロントに絞る(A)。#45(数字ごと)のフロントは作らず Issue を閉じた(Worker 側 `ask:"where"` は API として残す)。記録の識別子は `m` に聞き方を含める別名方式(`typesafe/jev/all`、Claude は `<モデル識別子>/all`)
- 2026-09-22: オーナー要望で「処理時間(ミリ秒)」(#55)と「コスト(ドル)」(#56)を起票。Worker 側 `usage`(PR #57、Opus 実装・レビュー)をマージ・デプロイ(Version `7616ba22`)。本番 curl で 200 に `usage: {input_tokens: 665, output_tokens: 80}` が載ることを確認
- 2026-09-22: 一括モードのフロント(PR #58、Sonnet 実装・Opus レビュー)をマージ・デプロイ(Version `92a11dd4`)。レビューの should-fix 3 件(`<details>` の開閉保持・応答のキー検証を確定前に・SPEC の一括節)と nit を反映。本番で「一括」+「最速」を curl 中継の Playwright で 1 周: `/api/judge` 1 回(`ask:"all"`)、1,554 ms、51 マス色付き、エラーなし。Claude 経路の一括(`max_tokens` 下限)は実キー未検証(オーナー確認待ち、#44 と同じ)
- 2026-09-23: オーナー報告「同じ数字を入れ続けて 15 周まで直らない」「完了/強制終了バナーを見逃した」「統計カードの 100% が正解率に見える」。バナーはページ最上部へ(PR #62、Version `a6498c85`)。統計カードは #60(#61/#63 のフロントと同じ PR で)。消去法は #61(履歴)・#63(ルール候補: 行・列・ブロックの既出数字を外す)・#64(局所文脈: 質問文に行・列・ブロックの中身を添える。実験)に分けて起票
- 2026-09-23: #61 の Worker 側 `exclude`(PR #65、Opus 実装・レビュー)をマージ・デプロイ(Version `474189a4`)。`digit` は配列、`all` はマスごとのマップ、`cell` / `where` は 400。criteria と応答検証を残りの数字にし、`exclude` 時の 502 文言は「候補の数字(exclude後)…」。不変条件 1 に例外を明記(外れた数字だけを送る)。本番 curl: `r0c2` を `exclude:["1","2"]` で 200 / 2.3 秒、criteria は 3〜9、`choice` 4(正解)。フロントは #63 と同じ PR(#55/#56 の後)
- 2026-09-22〜23: セッションの利用制限(Opus/Sonnet)でサブエージェント 3 つが途中終了した。制限解除後に `SendMessage` で同じエージェントを再開し、worktree の未コミット変更から続行できた(worktree は残っているので作業は失われない)
- 2026-09-23: #55(処理時間)+ #56(コスト)のフロント(PR #67、Sonnet 実装・Opus レビュー)をマージ・デプロイ(Version `66b9572b`)。レビューの must-fix 1 件(一括モードで確定待ち中に停止 → 再開すると usage が消える → `commitFocused()` で消費)、should-fix 4 件(Sonnet 5 の単価を 2/10 に・停止で捨てた応答済みぶんの持ち越し `carryUsage`・計時テスト AA10・docs)を反映。旧記録の `t`(記録時刻)は `at` に改名し、読み込み時に付け替える(`migrateLegacyRecordTimestamps`)。本番で「一括」+「最速」1 周: `1周目: 51中12正解 (24%) — 3,791 ms / $0.0004`。次は #61/#63/#60 のフロント(Sonnet 実装中)
- 2026-09-23: オーナー報告(スクリーンショット添付、モデルを Sonnet 5 に変更)「単価パネルは定義で、実消費が見えない。TPS も含めて近くに出して」。Issue #69 を起票し、単価パネルの直上に「この実行の消費」パネル(累計トークン・コスト・平均 tok/s)を追加(PR #70)。レビューで速度の分母を演出待ち込みの経過時間から API 待ち時間(`totalLatencyMs`)に変更。マージ・デプロイ(Version `741300db`)。本番で一括モードを1周確認: `1周目: 51中12正解 (24%) — 3,776 ms / $0.0004`、消費パネルの描画を確認。オーナー追加要望「TPS は最速モードのときだけでいい」は #61/#63/#60 のブランチに追い込みコミットとして乗せ、PR #72 と一緒にマージ・デプロイ
- 2026-09-23: #61(履歴)・#63(ルール候補)・#60(統計カード分割)のフロント(PR #72、Sonnet 実装・Opus レビュー)をマージ・デプロイ(Version `1d5a73b8`)。オーナー要望「TPS は最速モードのときだけでいい」(#69 の追加)もこの PR に含めた。Opus レビューの must-fix 1 件(**M1**: `ruleExclusions()` が `state.values` を正誤問わず素通しで見ていたため、同じ周の中で先に(不正解のまま)確定した推測が別マスの候補から本当の正解を除外しうるバグ。`settledKeys`(周の開始時点でのスナップショット、前の周までに正解して確定したマスだけ)を導入して修正、回帰テスト AB11・AB12・AB14 を追加)、should-fix 2 件(docs の「正誤問わず」表記の訂正、統計カード5枚の CSS グリッドを2列固定→6列+span に変更)、nit 1 件(履歴コメントの誤り)を反映。`npm test` 282 pass、`npm run e2e` の3モードとも 0 件失敗を確認してからマージ。本番 curl で `exclude:["1","2","8"]` を渡すと `probabilities` / `criteria` からその3つが消えることを確認(約1.8秒)。`npm run e2e -- --url <URL>` の本番モードも PASS
- 2026-09-23: オーナー報告(スクリーンショット添付)「一括モードを Claude で動かすと Claude API HTTP 400: The compiled grammar is too large で止まる」。原因調査の結果、`buildClaudeAllSchema` がマス数ぶん独立した入れ子オブジェクトをスキーマに持つため、マス数が多い(固定問題で51マス)と Anthropic の構造化出力の grammar サイズ上限を超えることが判明。Issue #74 を起票し、`askAllClaude()` を `CLAUDE_ALL_CHUNK_SIZE`(既定10)件ずつのチャンクに分けて並列に呼ぶ形に修正(PR #75、Sonnet 実装・Opus レビュー)。レビューの should-fix 4 件(部分失敗時に成功ぶんの usage を失わないよう `Promise.all`→`Promise.allSettled` に変更・失敗チャンクの表示・チャンクサイズ未検証の注記・コスト増の docs 追記)、nit 2 件を反映してからマージ・デプロイ(Version `db8138f7`)。Claude 経路はこのセッションにキーが無く実キー疎通は未検証(#44 に追記)。Jev 経路(Worker)は無関係なので本番の `/api/status` と E2E 本番モードで動作確認済み
- 2026-09-23: オーナー報告(スクリーンショット添付)「設定を横列にしてプルダウン化、消費パネルを盤面の下に、統計を1行に、単価パネルは不要、現在の判定を優先、所要時間も出してほしい」。Issue #76 を起票し、実装(PR #78、Sonnet 実装・Opus レビュー)。実行系ボタン+6トグルを完了バナーの下・盤面の上の横並びバーにまとめ、選択肢トグルをボタン群から `<select>` に変更。単価パネルを画面から削除(`renderPricesPanel()`/`setPrice()`/`resetPrices()`/`savePrices()` を削除、`getPrices()`/`costOf()` は読み取り専用でそのまま)。「この実行の消費」を盤面の直下へ、統計5項目を1行に、「現在の判定」→「周回ログ」の優先順位を強調(プロンプト枠を周回ログの後ろに)、完了/強制終了バナーに所要時間を追加。Opus レビューの should-fix 1 件(**S1**: `render()` が実行中に `innerHTML` を丸ごと作り直すため、高頻度な再描画(一括モードの確定ごとなど)で速度/難易度の `<select>` のプルダウン/フォーカスが奪われ選べなくなる回帰。`isControlSelectFocused()` で select にフォーカスがある間 `render()` を丸ごとスキップするよう修正)と nit 4 件を反映してからマージ・デプロイ(Version `271fc56f`)。本番の E2E([R] 最速実行)で新しい `<select>` の `selectOption` 操作が実機で通ることを確認、スクリーンショットで新レイアウトの描画を確認
- 2026-09-23: オーナー要望3件。(1)「一括で最速のとき、マスごとの描画をなしにして本当の最速ができるように」→ 一括+最速のときだけ選べる「描画: 毎回表示 / 省略(最速)」トグルを追加。(2)「判定中の表示の右の空いたスペースに開始から終了までの時間をミリ秒単位で」→「現在の判定」パネルの見出し行の右に経過時間を常時表示、重複していたコスト表示は削除。(3)「比較モードで Claude が動いているか分からない」→ 埋め込み iframe の `postStatus()` に直近のリクエストを乗せ、比較シェルの各カラムに「モデルに送ったプロンプト」と同じ内容を表示。Issue #80 を起票し、実装(PR #81、Sonnet 実装・Opus レビュー)。`commitFocused()` の本体を `commitJudgment()` として切り出し、`commitAllInstant()` が `render()` を挟まずループで確定できるようにした。Opus レビューの **M1(must-fix)**: `render()` の select フォーカスガード(PR #78 由来)が `state.running` を見ずに常時効いていたため、select は選んだあともフォーカスを保つブラウザの挙動と組み合わさると、実行前に速度→順番と select を渡り歩くだけで render() が一度も走らず instant-toggle の無効化状態が更新されない(一括+最速を選んでも有効に見えない)問題があった。ガードを `state.running` のときだけに絞って修正。**S1(should-fix)**: 比較シェルの左上から/確信度順モードのプロンプト表示が折りたたみの無い生の `<pre>` だったため、Jev/Claude の応答タイミングがずれると2カラムの縦幅が食い違い iframe がずれる問題があり、比較シェルだけ折りたたむよう修正。nit 2 件(一括モード専用の空表示文言・render 回数の見積もり)も反映してからマージ・デプロイ(Version `35a0566f`)。デプロイ後の本番確認(`npm run e2e -- --url <URL>`)で E2E 本番モードの `[R]` チェック自体のバグ(`page.waitForFunction(fn, {timeout})` の第2引数が `options` ではなく `arg` として渡っていたため既定30秒でタイムアウトしていた。本番の Jev 呼び出しは1回あたり最大20秒台になることがあるため実際に超過していた。#81 自体のコードに問題は無い)を発見し、別 PR #82 で修正・マージ(Version は不変、`src/index.js` を含まないため再デプロイ不要)。本番で instant-toggle のライブ有効化・一括+最速の動作・比較モードのプロンプト表示(Jev は「▶ プロンプトを表示」の折りたたみ、Claude はキー未設定で「まだ判定していません」)を Playwright で目視確認済み
- 2026-09-23: オーナー追加要望2件。(1)「現在判定の経過時間だけじゃなく、実行開始から終了までのトータル時間をリアルタイム表示してほしい」→ #80/#81 で追加した経過時間表示が `render()` 頼みで、一括モードの応答待ち中などは止まって見えていた問題への対応。(2)(スクリーンショット添付)「保存済み末尾の表示は不要。モデル名選択とAPIキーのUIを分離して、APIキーは折りたたんで隠して」→ Claude 設定パネルの再構成。Issue #84 を起票し、実装(PR #85、Sonnet 実装・Opus レビュー)。`startElapsedTicker()` が `run()` で `setInterval(100ms)` を張り、`render()` を丸ごと呼ばず `#current-elapsed` の `textContent` だけを書き換える(select フォーカスガードや一括モードの描画省略と無関係に常に動く)。`renderClaudeSettings()` はモデル選択・思考トグルを常に見える行に、API キー入力を既定で閉じた `<details>` に分離し、末尾4文字のヒント表示(`anthropicKeyHint()`)は関数ごと削除した。Opus レビューの should-fix 3 件(**S1**: setInterval が全停止経路で止まることのテストが無かった→AD10 追加。**S2**: SPEC の記述が古い挙動のまま→修正。**S3**: 比較シェルの見出しの経過時間が一括モードの応答待ち中は止まって見える→ティックから10回に1回 `postStatus()` するよう修正、AD11 追加)と nit 4 件を反映してからマージ・デプロイ(Version `8010726c`)。本番で Playwright により、経過時間が実時間どおりに動くこと(1.5秒待って `2 ms`→`1,501 ms`)と、Claude 設定パネルの折りたたみ挙動をスクリーンショットで確認済み
- 2026-09-23: オーナー要望「数独の難易度で、むずかしい＋とか、まだ難しいやつを作ってほしい。にこりさんの数独のレベル感でいうと上級に当たるものを出してほしい」(中級30分・上級1時間という体感の目安つき)。Issue #87 を起票し、`node:vm` ベンチマークで検証: `MIN_TARGET_GIVENS=24` がそれより低い目標指定を無効化していたこと、その下限を外しても `generatePuzzle()` 単体(1回勝負の貪欲法)は目標をどれだけ下げても実質22〜29あたりが到達限界であること、`GENERATE_RETRIES` を3→20に上げると目標22(上級)が22〜23ヒントに安定して収束することを確認。実装(PR #88、Sonnet 実装・Opus レビュー)。`DIFFICULTY_GIVENS` に `expert: 22`(にこりの「上級」相当)を追加、`MIN_TARGET_GIVENS` を理論上の最小値17に、`GENERATE_RETRIES` を20に変更。Opus レビューの **must-fix**(「難易度ごとの generatePuzzle」テストの上級ケースが単発呼び出しで上限27を取っていたため、5000回のベンチマークで最大29まで観測されている実態と食い違い約5%の確率で flaky だった → 上限を35に緩和)、**should-fix** 3件(下限テストが旧値24をハードコードしたままで新しい下限17を実際には検証していなかった → 目標10を渡す形に書き換え/「むずかしいも収束しやすくなった」という当初の記述が実測(単発でも約85%の確率で25ちょうどに届く)と食い違っていた → 訂正/「上級は10回の生成に2.5〜3.6秒」という記述が `node:vm` テストハーネス経由の計測値でブラウザ実機の値ではなかった → 実機推定値(1回あたり平均60ms・最悪150ms程度)と書き分け)を反映し、`npm test` を3回連続実行して flaky が無いことを確認してからマージ・デプロイ(Version `b8b0cb2f`)。本番で Playwright により、難易度セレクトに「上級」が追加されていること・選択して「新しい問題」を押すと23ヒントの盤面が生成されることをスクリーンショットで確認済み
- 要オーナー判断(未対応。日付なしの定常項目): (1) 確信度順は 1 マス 2 回呼ぶので、1 周ぶん(約 102 回)で IP 上限 120 回/時をほぼ使い切り、1 問(約 156 回)は 1 時間で完走できない。上限を緩めるか(作業ルール 4)→ **#20**(追記済み)。(2) Claude 経路の実キー疎通(CORS ヘッダー、structured outputs + thinking、Haiku の budget_tokens)はオーナーのブラウザで確認が必要 → **#44**(手順あり)。(3) 一括モード(Claude 経路)のチャンク分割(#74)の `CLAUDE_ALL_CHUNK_SIZE`(既定 10)は実キーで確かめた値ではなく保守的な推測値。特に「むずかしい」(空マス最大56)+ 消去法の両トグルなし(候補が絞られず1マスあたりのスキーマが最大)+ Opus 思考ありの組み合わせで、10 件ずつでも 400 を再発しないか実キーで確認してほしい → **#44** に追記予定(実キーでの疎通確認とまとめて)。(4) 「上級」(#87、目標22)の生成時間(ブラウザ実機で1回あたり平均60ms・最悪150ms程度、Opus レビューのベンチマーク)が UX として許容範囲かはオーナー判断が必要 → **#87**(「生成中…」表示でブロッキングは隠しているが、実際にオーナーが触って確認してほしい)
- 次にやること: オーナーがブラウザで 1 問実行して較正図を確認する(SPEC 6 章の「デプロイ後」項目の目視分)。拡張 4(ログ異常検知への転用)は依頼があってから Issue を起こす(拡張 3 の Claude API は #37 で実装済み)。知見のメモ: 同じ入力でも Jev の `choice` は揺れ、`probabilities` はほぼ平坦(最大 0.2 前後)なので、15 周で収束しないことが多い。較正図は `pc` と `conf` の 2 枚を比べるのが実験の核心

## 7. 次のセッションで貼るプロンプト

````text
CLAUDE.md と docs/ の4ファイルを読んでから、docs/HANDOFF.md 2章の自律ループを再開してください。
まず docs/HANDOFF.md 1章の人間作業の状態を確認し、ブロックされている Issue があれば
何が必要かを最初に報告してください。着手できる Issue があれば、3章のモデルの使い分けに
従って実装・テスト・レビュー・マージまで進め、終わったら CLAUDE.md と HANDOFF.md 6章を更新してください。
````
